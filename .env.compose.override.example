# Optional: only if default Docker host ports 3200 (web) / 3201 (API) are already in use.
# Merge with your main .env:
#   docker compose --env-file .env --env-file .env.compose.override up -d

WEB_PORT=3250
API_PORT=3251
